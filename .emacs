;; Define the init file
;; (setq custom-file (expand-file-name "lisp/custom.el" user-emacs-directory))
 (setq custom-file "~/.config/emacs/emacs-custom-eric.el")
  (when (file-exists-p custom-file)
    (load custom-file))



